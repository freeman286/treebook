<%= devise_error_messages! %>

<h2 class="page-header">Resend unlock instructions</h2>

<%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post, class: "well" }) do |f| %>

  <div><%= f.label :email %><br />
  <%= f.email_field :email, :autofocus => true %></div>

  <div class="form-actions"><%= f.submit "Resend unlock instructions", class: "btn" %></div>
<% end %>

<%= render "devise/shared/links" %>
